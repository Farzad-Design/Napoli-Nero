import type { Metadata } from 'next';
import { Cormorant_Garamond, Montserrat } from 'next/font/google';
import './globals.css';

const cormorant = Cormorant_Garamond({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  style:  ['normal', 'italic'],
  variable: '--font-display',
  display: 'swap',
});

const montserrat = Montserrat({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  variable: '--font-body',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Napoli Nero — Luxury Italian Pizza Restaurant',
  description:
    'Experience authentic Italian flavor crafted with premium ingredients, wood-fired perfection, and a modern luxury dining experience. An elevated pizza experience where Naples tradition meets modern luxury.',
  keywords: 'Italian pizza, luxury dining, Naples, wood-fired pizza, fine dining',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${cormorant.variable} ${montserrat.variable}`}>
      <body>{children}</body>
    </html>
  );
}
